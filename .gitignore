node_modules/
.next/
dist/
*.tsbuildinfo
.env
.env.local
.env.*.local
*.log

# Stray local artifacts — plaintext DB creds and raw Postgres dumps (contain
# real tenant/payment PII). Never commit these; flagged to user, not deleted.
backend/env
backend/backend/
backend/infinite
backend/infinite (1)
backend/infinite2
backend/inifiinte3
backend/current-state.sql

# Local Claude Code tool settings (machine/session-specific, not project config)
.claude/
